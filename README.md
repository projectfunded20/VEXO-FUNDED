# Prop Firm Showcase

I am attaching a ZIP file containing the source code of my existing prop-firm website.

For this FIRST step, I only want you to analyze and recreate the existing frontend/UI.

IMPORTANT:

Extract and inspect the entire ZIP before making any changes.

Identify all existing frontend pages, components, layouts, navigation, dashboards, cards, tables, forms, buttons, modals, pricing sections, challenge/account sections, FAQ sections, footer, header, responsive layouts, and all other visible UI elements.

Recreate the existing frontend UI inside this Lovable project so that I can properly preview and navigate the website.

Preserve the existing visual structure, layout, spacing, typography, components, interactions, and responsive behavior as closely as reasonably possible during this first phase.

Do NOT copy, expose, or reuse any private API keys, passwords, secrets, database credentials, Firebase credentials, payment credentials, or environment secrets from the ZIP.

Do NOT connect to the existing backend.

Do NOT migrate, modify, or depend on the existing backend.

Do NOT add Resend, authentication, payments, databases, webhooks, or other backend services yet.

Wherever backend data is required for the UI, use realistic mock/static data so every page can be previewed and demonstrated.

Make sure the website works properly on both desktop and mobile screens.

Keep the code clean, reusable, componentized, and maintainable. Do not put the entire application into one huge component or file.

If the existing project uses React/Vite, preserve that frontend architecture unless there is a strong technical reason not to.

Do NOT redesign the website at this stage.

Do NOT change the existing branding, colors, pricing, account rules, text, or business logic yet unless something is absolutely required for the UI to run.

If you find existing account prices, drawdown/loss limits, profit targets, challenge rules, or similar information in the source code, reproduce them as mock/static UI data for now. Do not create backend logic for them yet.

If some parts of the original frontend depend on APIs or backend responses, create appropriate mock data/interfaces so the frontend can still render and be tested.

Do not delete existing useful frontend functionality simply because its backend is unavailable. Replace backend-dependent data with mock data where necessary.

THE GOAL OF THIS STEP:

ZIP → inspect the existing project → identify the frontend → recreate the complete UI → connect pages/navigation → use mock data where necessary → make everything fully previewable in Lovable.

Do not implement the new branding, new website name, backend, authentication, payments, email system, Resend, or database yet.

I will provide those instructions in the NEXT step after I review the recreated UI.

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/d4b6037e-b2d8-4160-a495-87273e6609ed).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
